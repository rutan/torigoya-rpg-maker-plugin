{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "_dist/**", "src/_build/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
